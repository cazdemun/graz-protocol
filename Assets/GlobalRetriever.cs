using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalRetriever : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hola");
        Debug.Log(Globals.MiVar);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
